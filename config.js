// The evaluator API endpoint. Updated once the Cloudflare Worker is deployed.
// Until then the page loads and renders, but submitting an answer will fail.
window.EVAL_API = 'https://upscdesk-eval.workers.dev';
